<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pokemon Binder</title>
  <link rel="stylesheet" href="" />
</head>
<body>
  <div class="wrapper">
    <header>
      <h1>
         <a href="/api/"> Pokemon Binder</a>
      </h1>

      <nav>

        {{#if loggedIn}}
        <a>Search</a>
        <buton id="logout" class="logout-btn">Logout</buton>
        {{else}}
        <a href="/login">Login</a>
        <a href="/">SignUp</a>
        {{/if}}

      </nav>
    </header>
    <main>

        {{{body}}}

    </main>
  </div>
  
</body>
</html>